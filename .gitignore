node_modules/

# Local, temporary working state pending human validation — not the source
# of truth. Once a question is validated it's moved to data/validated/,
# which is committed. The directory itself (and its README) stays tracked;
# only the generated content is ignored.
data/needs-review/*.json
data/needs-review/*.png
